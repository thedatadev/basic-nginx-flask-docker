## Instructions

> cd basic-flask-nginx-docker

> docker build -t hello-world .

> docker run --name hello-world --rm -it -p 80:80 hello-world

> Send a GET request to http://0.0.0.0/

> Verify the response payload reads "Hello, world!"